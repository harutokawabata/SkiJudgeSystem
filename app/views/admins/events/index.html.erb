<div class="container">
  <div class="row">
    <i class="fas fa-plus-circle">a<%= link_to new_admins_event_path %></i>
  </div>
  <div class="row">
      <% a = 0 %>
      <% @events.each do |event| %>
       <%= a += 1 %><%= event.title %><br>
       <%= event.event1 %>
      <% end %>
  </div>
</div>
